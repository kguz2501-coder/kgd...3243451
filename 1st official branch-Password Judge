<head>
  <title>Password Judge</title>
  <meta name = "viewport" content="width=device-width,initial scale=1.0">
</head>

<body>

  <main>
  
  <h1>Password Judge</h1>
  
  <section class="card info">
    <h3>How to create a strong password:</h3>
    <ul>
   <li>Use at least 8 characters - the more the better!</li>
   <li>Mix uppercase and lowercase letters</li>
   <li>Include at least one number (0-9) </li>
   <li>Add a symbol or special character(!@#$%^&*())
   </ul>  
  </section>
  
  </main>
    
</body>
   
  <label for="passInput"> Enter password:</label>
  
  <div class= "inputContainer">
    
    <input type="password" id="passInput" placeholder="Type here...">
    <br>
    <input type="checkbox" id="showpass"> Show Password
  
    </div>
  
  <p>Strength:
  <div id="strengthText">Waiting...</div>
  </p>
  
  </section>
  
</body>

body {
    font-fammily: 'Segow UI', sans serif;
  background-color: #f0f2f5;
  display: flex;
  justify-content: center
  padding-top: 50px;
}

main {
  width: 100%;
  max-width: 500px;
  text-align: center;
}

.card {
background: white;
padding: 20px;
margin-bottom: 20px;
border-radius: 12px;
box-shadow: 0 4px 6px rgba(0,0,0,0.1);
}

.info {
  text-align: left;
  font-size: 0.9rem;
  line-height: 1.5;
}

input[type="password"], input[type="text"] {
  padding: 1opx;
  width: 80px;
  border: 2px solid #ddd;
  border-radius: 6px;
  margin-bottom: 10px;
}

const passwordInput = document.getElementbyId('passInput');

const strengthText = document.getElementbyId('strengthText');
 
const hasNumber = /\d/; //Check for any digit

const hasSpecial = /[!@#$%^&*(),.?"{}|<>]/; // Check for any of these symbols

passwordInput.addEventListener('input', () => {
    const val = passwordInput.value;
  
  // 1. Check for specific requirements
  const longEnough = val.length >= 8;
  const containsNumber = hasNumber.test(val);
  const containsSpecial = hasSpecial.test(val);
  
  // 2. The Judges Decision
  if (val.length === o) {
    strengthText.innertext = "Waiting...";
    strengthText.style.color = "black";
  } else if (val.length < 6) {
  strengthText.innertext = "Weak";
  strengthText.style.color = "red";
  } else if (val.length >= 6 && val.length < 10) {
  strengthText.innertext = "Medium";
  strengthText.style.color = "orange";
  } else {
  strengthText.innertext = "Strong";
  strengthText.style.color = "Green";

    
    const showPassCheckbox = document.getElementbyId('showPass')
    
    showPassCheckbox.addEventListener('change', () => {
    // If the checkbox is checked, type is 'text', otherwise 'password'
    if (showPassCheckbox.checked) {
      passwordInput.type = 'text';
    } else {
      passwordInput.type = 'password';
    }
});
